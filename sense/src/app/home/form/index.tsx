import React from "react";
import FormPage from "../../components/Form"; // Import the FormPage component

const IndexPage = () => {
  return (
    <div>
      <FormPage /> {/* Render the FormPage component */}
    </div>
  );
};

export default IndexPage;
